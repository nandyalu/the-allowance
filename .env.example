# Copy to .env and fill in. See docs/setup.md for how to obtain each value,
# and compose.example.yaml for how they reach the container.

# --- Webull: required ---
# The agent's simulated account, and real-time quotes. Get an app key at
# developer.webull.com and take the PAPER pair, not the production one.
WEBULL_APP_KEY=
WEBULL_APP_SECRET=

# Which simulated account this deployment owns, as the account number (DE…)
# or the internal account id. Required, with no default on purpose: unset or
# empty and the app places no order at all.
#
# Three checks already narrow the sandbox's accounts to one — the sandbox
# flag, the DE number prefix, and the account class — and they would narrow to
# the SAME one for every deployment applying the same rule. Two containers
# would then trade one book, and afterwards nothing could say which of them
# placed an order. The value of this line is that a person wrote it down.
WEBULL_ACCOUNT_ID=

# A cash account refuses a short outright. The app enforces long-only itself
# as well, rather than inheriting it from the account type.
WEBULL_ACCOUNT_CLASS=INDIVIDUAL_CASH

# --- the model: required, and any one of three shapes ---
# The endpoint must answer before an analysis can run. The setup page at
# /setup reports whether it does.

# 1. A local Ollama pool. Free beyond electricity, and slow — about 19
#    minutes an analysis on an 8 GiB card.
TRADINGAGENTS_LLM_PROVIDER=ollama
OLLAMA_BASE_URL=http://host.docker.internal:11434/v1

# 2. Google Gemini. About 1.2 to 1.6 minutes an analysis.
#    TRADINGAGENTS_LLM_PROVIDER=google
#    GOOGLE_API_KEY=
#    LLM_MODEL=gemini-3.5-flash-lite

# 3. Any OpenAI-shaped endpoint — Cerebras, vLLM, LM Studio, a relay.
#    TRADINGAGENTS_LLM_PROVIDER=openai_compatible
#    TRADINGAGENTS_LLM_BACKEND_URL=https://api.cerebras.ai/v1
#    OPENAI_COMPATIBLE_API_KEY=
#    LLM_MODEL=qwen-3.8-27b

# The model name, shared by both think stages. It is also a database setting,
# so the settings page changes it without a redeploy; this is only the
# starting value.
#
# Choose on behaviour, not speed. Five small models were rejected here for
# answering fluently with prices they never fetched, and the fastest of them
# was the worst. See docs/deploying.md.
LLM_MODEL=gemma4-e4b-qat-128k

# Set this to your GPU count and no higher. One analysis is internally
# sequential, so it occupies exactly one card.
MAX_CONCURRENT_ANALYSES=1

# --- the experiment's own numbers ---
# Defaults for an unset setting only. The settings page wins once anyone
# changes them.
AGENT_BUDGET=10000
RESEARCH_PRICE_USD=0.05

# Leave empty. The start date is stamped automatically the first time the
# agent is switched on. Set it only to state a date the stamp cannot know —
# a deployment restored from a backup, say.
EXPERIMENT_START_DATE=

# --- optional ---
# Notifications. A webhook URL copied from the channel's own settings, not a
# bot: the app only ever posts, so there is no token and nothing to
# authenticate as. Unset means no notifications, and the site is identical.
DISCORD_WEBHOOK_URL=

# Free key from fred.stlouisfed.org, issued immediately. Without it the news
# analyst infers rates and the yield curve from headlines instead of reading
# the actual series, and says so in its own report.
FRED_API_KEY=

# --- Reddit OAuth2: leave blank ---
# Reddit's Responsible Builder Policy ended self-serve API app creation, so
# these cannot be obtained for a personal tool. Unset is the supported path:
# the sentiment analyst reads the public RSS search feed instead, and the
# 429 warnings that produces are expected. See docs/setup.md.
REDDIT_CLIENT_ID=
REDDIT_CLIENT_SECRET=
